# Textbook (Docusaurus) Environment Variables
# For LOCAL DEVELOPMENT ONLY - copy to .env.local

# Note: These are used at BUILD TIME in docusaurus.config.ts
# For Vercel deployment, the production URLs are hardcoded in docusaurus.config.ts

# Local development URLs
AUTH_URL=http://localhost:3001
API_URL=http://localhost:8000
